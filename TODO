Version 0.3:

* When the cluster is really busy, dx messages might be appended
  to WCY, WWV or To All messages. We have to split them.
* Statusbar messages other than "Connected to .." or "disconnected"
  should get at timeout.
* Clickable URL's.
* Clean up the icon for the debian menu (lintian).

Version 0.4:

* Interface with hamlib.
* Filters for dx messages.

